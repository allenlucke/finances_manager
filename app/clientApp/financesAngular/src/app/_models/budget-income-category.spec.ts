import { BudgetIncomeCategory } from './budget-income-category';

describe('BudgetIncomeCategory', () => {
  it('should create an instance', () => {
    expect(new BudgetIncomeCategory()).toBeTruthy();
  });
});
